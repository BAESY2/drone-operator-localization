# Contributing

1. Keep predictions on live public APIs. Do not add synthetic/mock geospatial datasets.
2. Identify the client with `User-Agent` for OSM/Overpass.
3. Run `pytest tests/ -v` before opening a PR.
